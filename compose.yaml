services:
    api:
        build:
            context: ./api
            dockerfile: Dockerfile
        ports:
            - "3000:3000"

    web:
        build:
            context: ./web
            dockerfile: Dockerfile
        ports:
            - "80:80"
        depends_on:
            - api
